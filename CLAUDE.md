# CLAUDE.md

BitCal 采用轻流程、小而快策略。默认行为如下：

## 会话规则

- 与维护者统一中文沟通
- 串行推进，避免上下文分裂
- 大改优先做减法

## 工程规则

- 以 `include/bitcal/` 与 `tests/test_bitcal.cpp` 为主真相
- 公开 API 或行为变化必须同步 README 与测试
- 删除无调用代码与无价值自动化

## 文档规则

- 不新增重复 Markdown/HTML 镜像
- docs 聚焦高价值导航与说明
- 双语范围控制在必要入口

## 工具链规则

- `AGENTS.md` 是跨代理规则
- 本文件是会话级补充偏好
- 仅保留高价值 `.claude/` 入口

## 审查规则

- 阶段性大改前优先 `/review`
- 若发现漂移，先收敛一致性，再扩展
