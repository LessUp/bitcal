## 1. Spec truth

- [x] 1.1 创建 `2026-05-14-vnext-cpp23-x64-redesign` 变更目录，并写 proposal / design / tasks / spec deltas
- [x] 1.2 同步持久 OpenSpec 与 RFC 0001 到 vNext 契约

## 2. Public skeleton

- [x] 2.1 将项目公开基线提升到 C++23
- [x] 2.2 新增 `bit_block`、`bit_view`、`algorithms` 和 `detail/x64_dispatch` 的最小可编译公共骨架
- [x] 2.3 用 contract smoke tests 验证新公共骨架

## 3. Entry points

- [x] 3.1 更新示例、README 与项目状态文档，使入口叙事切换到 vNext
- [x] 3.2 确认后续 correctness / benchmark / Pages 工作基于新契约继续推进
